---
phase: 06-model-state-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - api/cleanup.js
  - api/blueprint.js
  - vercel.json
autonomous: true
requirements:
  - MSF-01
must_haves:
  truths:
    - "`api/blueprint.js` no longer exists in the repo (D-11 hard delete)"
    - "`api/cleanup.js` PREFIXES array contains `'model-state/'` so state Blobs expire on the 7-day cron"
    - "`vercel.json` functions block references `api/model-state.js` (60s maxDuration) and does NOT reference `api/blueprint.js`"
    - "No remaining import or reference to `./blueprint` anywhere in the codebase"
  artifacts:
    - path: "api/cleanup.js"
      provides: "Updated PREFIXES array including model-state/"
      contains: "model-state/"
    - path: "vercel.json"
      provides: "Functions config aligned with v3.0 endpoint set"
      contains: "model-state"
  key_links:
    - from: "api/cleanup.js"
      to: "Vercel Blob model-state/ prefix"
      via: "PREFIXES array entry"
      pattern: "'model-state/'"
    - from: "vercel.json"
      to: "api/model-state.js"
      via: "functions config entry"
      pattern: "api/model-state.js"
---

<objective>
Complete the v2.0 → v3.0 infrastructure swap:
  1. Delete `api/blueprint.js` entirely (D-11 hard delete — no parallel running).
  2. Add `'model-state/'` to the `PREFIXES` array in `api/cleanup.js` so state Blobs participate in the existing 7-day TTL cron (D-07).
  3. Update `vercel.json` functions config to reflect the new endpoint set (remove `blueprint`, ensure `model-state` is configured if explicit entry was used; otherwise verify auto-detection still works).
  4. Confirm no remaining references to `blueprint` exist in the API directory.

Purpose: Removes dead code, prevents two endpoints from being deployed simultaneously, and keeps Blob storage costs bounded.

Output: A clean v3.0 backend file set with no v2.0 blueprint endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-model-state-foundation/06-CONTEXT.md
@.planning/phases/06-model-state-foundation/06-RESEARCH.md
@api/cleanup.js
@api/blueprint.js
@vercel.json
@api/model-state.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add model-state/ prefix to cleanup.js and delete blueprint.js</name>
  <files>api/cleanup.js, api/blueprint.js</files>
  <read_first>
    - api/cleanup.js (current PREFIXES array — confirm exact line and existing entries to preserve)
    - api/blueprint.js (so we can confirm scope of what is being deleted; check for any unique helper functions that might be needed elsewhere — they should NOT be, per the research)
  </read_first>
  <action>
    Step A — Modify `api/cleanup.js`:
    Find the line declaring `const PREFIXES = [...]` (around line 8 per research). The current value is approximately:
    ```
    const PREFIXES = ['reports/', 'blueprints/', 'analysis-cache-v14/', 'analysis-narrative-cache-v2/'];
    ```
    Append `'model-state/'` to the array. Preserve all existing entries (the `blueprints/` entry stays so v2.0 Blobs continue to expire per D-10). New value:
    ```
    const PREFIXES = ['reports/', 'blueprints/', 'analysis-cache-v14/', 'analysis-narrative-cache-v2/', 'model-state/'];
    ```

    Step B — Delete `api/blueprint.js`:
    Remove the file entirely. Use the shell: `rm api/blueprint.js`.

    Do NOT modify `api/analyze.js` in this task — Phase 6 explicitly leaves it alone (per RESEARCH.md §Open Questions #2). The CSV fallback path and existing shared reports continue to use `analyze.js` with `blueprintUrl` until Phase 7 ships `analyze-v3.js`.
  </action>
  <verify>
    <automated>test ! -f api/blueprint.js && grep -c "'model-state/'" api/cleanup.js && grep -c "'blueprints/'" api/cleanup.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f api/blueprint.js` returns 1 (file does not exist)
    - `grep -c "'model-state/'" api/cleanup.js` returns 1
    - `grep -c "'blueprints/'" api/cleanup.js` returns 1 (legacy entry preserved per D-10)
    - `grep -c "'reports/'" api/cleanup.js` returns 1 (other entries preserved)
    - `grep -c "PREFIXES" api/cleanup.js` returns at least 2 (declaration + at least one usage)
    - `node -c api/cleanup.js` exits 0
    - `grep -r "from ['\"]\\./blueprint['\"]" api/ 2>/dev/null | wc -l` returns 0 (no remaining imports)
    - `grep -r "require.*blueprint" api/ 2>/dev/null | wc -l` returns 0
  </acceptance_criteria>
  <done>`blueprint.js` is gone. `cleanup.js` covers `model-state/` Blobs on the cron. No dangling imports.</done>
</task>

<task type="auto">
  <name>Task 2: Update vercel.json to reflect the v3.0 endpoint set</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json (read the entire current file to understand its structure — functions block, crons block, rewrites; per research it uses `rewrites` not `routes`)
    - .planning/phases/06-model-state-foundation/06-RESEARCH.md §Code Examples §vercel.json
  </read_first>
  <action>
    Read the current `vercel.json`. Make these exact changes:

    1. **Functions block:** If the file contains a `"functions"` object with an entry for `"api/blueprint.js"`, replace that key with `"api/model-state.js"`. Preserve the existing config (`"maxDuration": 60`). The replacement looks like:
       ```json
       "api/model-state.js": { "maxDuration": 60 }
       ```
       If `api/blueprint.js` is NOT in the functions block (auto-detected), then ADD a new entry for `api/model-state.js` with `{ "maxDuration": 60 }` alongside any other existing function configs. Either way, the final file MUST contain `"api/model-state.js"` and MUST NOT contain `"api/blueprint.js"`.

    2. **Rewrites block:** Per research, the file uses `rewrites` for routing. If there is a rewrite rule mentioning `/api/blueprint`, replace its `destination` with `/api/model-state`. If no such rule exists (the API is auto-routed by filename), leave the rewrites block unchanged.

    3. **Crons block:** No change. The cleanup cron is unchanged — only the PREFIXES array inside `api/cleanup.js` was updated in Task 1.

    4. **JSON validity:** After edits, the file MUST be valid JSON. Validate with `node -e "JSON.parse(require('fs').readFileSync('vercel.json', 'utf8'))"` — exit 0 means valid.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))" && grep -c "api/model-state" vercel.json && ! grep -q "api/blueprint" vercel.json</automated>
  </verify>
  <acceptance_criteria>
    - `vercel.json` is valid JSON (`node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"` exits 0)
    - `grep -c "api/model-state" vercel.json` returns at least 1
    - `grep -c "api/blueprint" vercel.json` returns 0
    - `grep -c "maxDuration" vercel.json` returns at least 1 (functions block intact)
    - `grep -c "crons" vercel.json` returns at least 1 (crons block intact)
  </acceptance_criteria>
  <done>`vercel.json` references `api/model-state.js` for the 60s function config and no longer references `api/blueprint.js`. JSON is valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel deploy → production | vercel.json controls function timeouts and routing — misconfiguration could expose endpoints with wrong limits |
| Cron job → Vercel Blob | cleanup.js iterates Blob prefixes with delete permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | vercel.json JSON syntax error breaks deploy | mitigate | Acceptance criterion validates JSON parses successfully; deploy pipeline will reject malformed config anyway |
| T-06-14 | Denial of Service | Wrong maxDuration on model-state.js | mitigate | Acceptance criterion enforces `maxDuration: 60` matches the Hobby plan ceiling; SSE handler designed to fit within this window |
| T-06-15 | Information Disclosure | Stale `blueprints/` Blobs retain old data after v3.0 deploy | accept | Per D-10, legacy Blobs expire naturally via 7-day TTL; `'blueprints/'` retained in PREFIXES so cleanup still applies |
| T-06-16 | Tampering | cleanup.js deletes Blobs from new prefix it should not own | mitigate | PREFIXES array is explicitly whitelisted; cron has no input from users; only deletes objects older than TTL (existing logic, unchanged in this plan) |
</threat_model>

<verification>
- `vercel.json` is valid JSON, contains `api/model-state`, contains no `api/blueprint`
- `api/cleanup.js` PREFIXES contains `'model-state/'` and still contains `'blueprints/'`
- `api/blueprint.js` does not exist
- No remaining `blueprint` imports anywhere in `api/`
</verification>

<success_criteria>
Backend file set matches the v3.0 target: `api/model-state.js` is the only model-fetch endpoint; `api/blueprint.js` is gone; Blob cleanup covers `model-state/`; Vercel function config aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/06-model-state-foundation/06-03-SUMMARY.md` recording:
  - The exact diff applied to cleanup.js PREFIXES
  - The exact diff applied to vercel.json
  - Confirmation that `api/blueprint.js` deletion did not leave dangling imports
</output>
